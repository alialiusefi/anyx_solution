spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: pass
  flyway:
    locations: filesystem:src/main/resources/flyway
app:
  s3:
    bucket-name: files-bucket
    url: localhost:4566
